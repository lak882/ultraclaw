# Python build artifacts
__pycache__/
*.pyc
*.pyo

# Runtime logs and scratch
sync.log
*.bak
*.current

# Nested git repositories in references/ (openclaw, ipm).
# These are upstream clones whose .git directories contain pack files
# over GitHub's 100MB per-file limit. The tree contents are tracked;
# only each nested repo's own .git/ metadata is excluded.
references/openclaw/.git/
references/ipm/.git/

# macOS junk
.DS_Store
references/superpowers/.git/
