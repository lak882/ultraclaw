# InterClaw Python dependencies
# Used by docx-to-md and pdf-to-md conversion in the chatbot upload flow.
# Installed by InterClaw.Installer.InstallPythonDeps() via irispython -m pip.
pdfplumber>=0.11
python-docx>=1.0
